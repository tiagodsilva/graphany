[project]
name = "graphany"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gcsbm",
    "jax>=0.11.1",
]

[tool.uv.sources]
gcsbm = { git = "https://github.com/tiagodsilva/gcsbm" }
